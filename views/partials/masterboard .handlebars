{{#each words}}
    <p data-id="{{id}}" name="tile-{{id}}" data-group="{{group_type}}" class="tile tile-master"> 
    {{!-- view css to be handled server side later --}}
        {{word}}
    </p>
{{/each}} 